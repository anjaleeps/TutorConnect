const dbManager = require('./StudentDBManager.js');

exports.StudentProfileBuilder = function (req, res, id ) {
    function build(req, res, id) {
        var profile = new Profile(id);
        
    }
}